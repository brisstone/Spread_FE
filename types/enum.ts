export enum EnterpriseRole {
  OWNER = "Owner",
  ADMIN = "Admin",
  LEAD = "Lead",
  OPERATOR = "Operator",
  READER = "Reader",
  CLIENT = "Client",
}

export enum StorageEnum {
  ENT_CONTEXT = 'enterprise_context',
}